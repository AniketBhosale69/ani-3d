# Paste-ready SQL for the Cloudflare dashboard

For the D1 **Console** tab, when setting up without the wrangler CLI.
Run them in order: 1, 2, 3, 4. Each file is self-contained.

Generated from seed.sql and verified to reproduce it exactly.
See DEPLOY.md for the CLI route, which loads seed.sql in one step.
